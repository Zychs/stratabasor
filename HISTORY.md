# History

Rebuilt from this sitting. Not from git. This folder had no commits.

## 2026-09-22 — first face

The bowser was named stratabasor so a tall directory would not keep growing inside the durable haven. The face inherited is AyTree's tree tool: folders in a repo, plus that repo's branches. The workbench list that hides branches is not this face. Spine, fullscreen, and parked keys landed the same day. A selected directory is a card over the dev and durable split, not a replacement of it. Git started the same day, when a public push was asked for.
